# 完整部署指南：GitHub + Streamlit Cloud

## 📋 部署前准备

### 需要的信息
- ✅ GitHub 用户名
- ✅ GitHub 邮箱
- ✅ GitHub 仓库名称（例如：`student-management`）

---

## 🚀 第一步：配置 Git 并提交代码

### 1.1 配置 Git 用户信息

在你的 PowerShell 中执行（替换为你的信息）：

```powershell
# 使用完整路径执行 Git 命令
$git = "D:\python\cusor\Git\bin\git.exe"

# 配置用户名和邮箱
& $git config --global user.name "你的GitHub用户名"
& $git config --global user.email "你的GitHub邮箱"
```

### 1.2 创建提交

```powershell
# 创建初始提交
& $git commit -m "Initial commit: 学生管理系统 - Streamlit版本"

# 设置主分支
& $git branch -M main
```

### 1.3 添加远程仓库

```powershell
# 替换为你的信息
$git = "D:\python\cusor\Git\bin\git.exe"
$username = "你的GitHub用户名"
$repoName = "你的仓库名"

& $git remote add origin "https://github.com/$username/$repoName.git"
```

---

## 📦 第二步：在 GitHub 上创建仓库

1. **访问 GitHub**
   - 打开：https://github.com/new
   - 登录你的 GitHub 账号

2. **创建新仓库**
   - **Repository name**: 输入仓库名称（例如：`student-management`）
   - **Description**: 学生管理系统（可选）
   - **Visibility**: 选择 Public（公开）或 Private（私有）
   - ⚠️ **重要**：不要勾选 "Initialize this repository with a README"
   - 点击 "Create repository"

---

## 🔑 第三步：生成 Personal Access Token

1. **访问 Token 设置**
   - 打开：https://github.com/settings/tokens
   - 点击 "Generate new token" → "Generate new token (classic)"

2. **配置 Token**
   - **Note**: 输入描述（例如：`Student Management Deploy`）
   - **Expiration**: 选择过期时间（建议选择较长时间）
   - **Select scopes**: 勾选 `repo` 权限
   - 点击 "Generate token"

3. **复制 Token**
   - ⚠️ **重要**：Token 只显示一次，立即复制保存！

---

## 📤 第四步：推送到 GitHub

在你的 PowerShell 中执行：

```powershell
# 使用完整路径
$git = "D:\python\cusor\Git\bin\git.exe"

# 推送到 GitHub
& $git push -u origin main
```

**输入信息**：
- **Username**: 输入你的 GitHub 用户名
- **Password**: 输入刚才生成的 Personal Access Token（不是 GitHub 密码）

---

## ☁️ 第五步：部署到 Streamlit Cloud

### 5.1 访问 Streamlit Cloud

1. 打开：https://streamlit.io/cloud
2. 点击 "Sign up" 或 "Sign in"
3. 使用 **GitHub 账号**登录

### 5.2 创建新应用

1. 点击右上角的 "New app" 按钮
2. 或直接访问：https://share.streamlit.io/deploy

### 5.3 配置应用

填写以下信息：

- **Repository**: 选择你的 GitHub 仓库
- **Branch**: 选择 `main`
- **Main file path**: 输入 `streamlit_app.py`
- **Python version**: 选择 `3.11`（推荐）

### 5.4 部署

1. 点击 "Deploy" 按钮
2. 等待部署完成（通常 1-2 分钟）
3. 部署成功后，你会获得一个网址，例如：
   ```
   https://你的应用名.streamlit.app
   ```

---

## ✅ 验证部署

### 检查 GitHub

1. 访问你的 GitHub 仓库页面
2. 应该能看到所有项目文件
3. 确认 `streamlit_app.py` 和 `requirements_streamlit.txt` 都在

### 检查 Streamlit Cloud

1. 访问你的 Streamlit 应用网址
2. 应该能看到学生管理系统界面
3. 测试添加学生功能

---

## 🔄 更新应用

以后更新代码时：

```powershell
$git = "D:\python\cusor\Git\bin\git.exe"

# 添加更改
& $git add .

# 提交更改
& $git commit -m "更新说明"

# 推送到 GitHub
& $git push
```

Streamlit Cloud 会自动检测 GitHub 的更新并重新部署！

---

## 🆘 常见问题

### Q: 推送时提示认证失败？

**解决方法**：
- 确保使用 Personal Access Token（不是密码）
- 确保 Token 有 `repo` 权限
- 重新生成 Token 试试

### Q: Streamlit Cloud 部署失败？

**检查清单**：
- ✅ `streamlit_app.py` 文件名正确
- ✅ `requirements_streamlit.txt` 存在
- ✅ 代码没有语法错误
- ✅ 查看 Streamlit Cloud 的日志（Logs 标签）

### Q: 如何查看部署日志？

1. 在 Streamlit Cloud 应用页面
2. 点击 "Logs" 标签
3. 查看实时日志

### Q: 如何更新应用？

1. 修改代码
2. 提交并推送到 GitHub
3. Streamlit Cloud 会自动重新部署

---

## 📝 快速命令参考

### Git 命令（使用完整路径）

```powershell
# 设置 Git 路径变量
$git = "D:\python\cusor\Git\bin\git.exe"

# 查看状态
& $git status

# 添加文件
& $git add .

# 提交
& $git commit -m "提交说明"

# 推送
& $git push -u origin main

# 查看日志
& $git log --oneline
```

---

## 🎉 完成！

部署成功后，你的学生管理系统就可以通过互联网访问了！

**访问地址示例**：
- GitHub: `https://github.com/你的用户名/你的仓库名`
- Streamlit: `https://你的应用名.streamlit.app`

---

需要帮助随时告诉我！🚀

