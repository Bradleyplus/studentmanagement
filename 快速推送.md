# 快速推送指南

## 🚀 方法一：使用自动化脚本（推荐）

1. **运行脚本**
   - 右键点击 `初始化Git.ps1`
   - 选择 "使用 PowerShell 运行"
   - 按提示输入信息

2. **在 GitHub 创建仓库**
   - 访问：https://github.com/new
   - 输入仓库名称
   - **不要**勾选 "Initialize with README"
   - 点击 "Create repository"

3. **生成 Token**
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选 `repo` 权限
   - 生成并复制 Token

4. **推送代码**
   ```powershell
   git push -u origin main
   ```
   - 用户名：你的 GitHub 用户名
   - 密码：输入刚才生成的 Token

---

## 📝 方法二：手动执行命令

如果脚本无法运行，可以手动执行：

### 1. 配置 Git（首次使用）

```powershell
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"
```

### 2. 初始化仓库

```powershell
# 进入项目目录
cd "D:\新建文件夹 (2)"

# 初始化
git init

# 添加文件
git add .

# 提交
git commit -m "Initial commit: 学生管理系统"
```

### 3. 添加远程仓库

```powershell
# 替换为你的仓库地址
git remote add origin https://github.com/你的用户名/仓库名.git

# 设置主分支
git branch -M main
```

### 4. 推送到 GitHub

```powershell
git push -u origin main
```

---

## ⚠️ 重要提示

### 如果 Git 命令无法识别

1. **重启 PowerShell**
   - 关闭所有 PowerShell 窗口
   - 重新打开

2. **检查 PATH**
   - Git 通常安装在：`C:\Program Files\Git\bin`
   - 如果不在 PATH 中，需要手动添加

3. **使用 Git Bash**
   - 如果安装了 Git，可以使用 Git Bash
   - 在项目文件夹右键 → "Git Bash Here"

---

## 🎯 推送成功后

推送成功后，你可以：

1. **部署到 Streamlit Cloud**
   - 参考 `STREAMLIT_DEPLOY.md`
   - 或访问：https://streamlit.io/cloud

2. **查看 GitHub 仓库**
   - 访问你的仓库页面
   - 应该能看到所有文件

---

## 🆘 遇到问题？

### 问题：提示 "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/你的用户名/仓库名.git
```

### 问题：推送被拒绝
确保 GitHub 仓库是空的，或者先拉取：
```powershell
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### 问题：认证失败
- 确保使用 Personal Access Token（不是密码）
- Token 需要有 `repo` 权限

---

需要帮助随时告诉我！🚀

